package android.app;

import android.os.Binder;
import android.os.IBinder;
import android.os.IInterface;
import android.os.Parcel;
import android.os.RemoteException;

public interface IColorStatusBar extends IInterface {

    public static abstract class Stub extends Binder implements IColorStatusBar {
        private static final String DESCRIPTOR = "android.app.IColorStatusBar";
        static final int TRANSACTION_onNavigationBarHide = 2;
        static final int TRANSACTION_onNavigationBarShow = 1;
        static final int TRANSACTION_onTransitionViewUpdate = 3;

        private static class Proxy implements IColorStatusBar {
            private IBinder mRemote;

            Proxy(IBinder remote) {
                this.mRemote = remote;
            }

            public IBinder asBinder() {
                return this.mRemote;
            }

            public String getInterfaceDescriptor() {
                return Stub.DESCRIPTOR;
            }

            public void onNavigationBarShow() throws RemoteException {
                Parcel _data = Parcel.obtain();
                try {
                    _data.writeInterfaceToken(Stub.DESCRIPTOR);
                    this.mRemote.transact(1, _data, null, 1);
                } finally {
                    _data.recycle();
                }
            }

            public void onNavigationBarHide() throws RemoteException {
                Parcel _data = Parcel.obtain();
                try {
                    _data.writeInterfaceToken(Stub.DESCRIPTOR);
                    this.mRemote.transact(2, _data, null, 1);
                } finally {
                    _data.recycle();
                }
            }

            public void onTransitionViewUpdate(int state) throws RemoteException {
                Parcel _data = Parcel.obtain();
                try {
                    _data.writeInterfaceToken(Stub.DESCRIPTOR);
                    _data.writeInt(state);
                    this.mRemote.transact(3, _data, null, 1);
                } finally {
                    _data.recycle();
                }
            }
        }

        public Stub() {
            attachInterface(this, DESCRIPTOR);
        }

        public static IColorStatusBar asInterface(IBinder obj) {
            if (obj == null) {
                return null;
            }
            IInterface iin = obj.queryLocalInterface(DESCRIPTOR);
            if (iin == null || !(iin instanceof IColorStatusBar)) {
                return new Proxy(obj);
            }
            return (IColorStatusBar) iin;
        }

        public IBinder asBinder() {
            return this;
        }

        public boolean onTransact(int code, Parcel data, Parcel reply, int flags) throws RemoteException {
            switch (code) {
                case 1:
                    data.enforceInterface(DESCRIPTOR);
                    onNavigationBarShow();
                    return true;
                case 2:
                    data.enforceInterface(DESCRIPTOR);
                    onNavigationBarHide();
                    return true;
                case 3:
                    data.enforceInterface(DESCRIPTOR);
                    onTransitionViewUpdate(data.readInt());
                    return true;
                case IBinder.INTERFACE_TRANSACTION /*1598968902*/:
                    reply.writeString(DESCRIPTOR);
                    return true;
                default:
                    return super.onTransact(code, data, reply, flags);
            }
        }
    }

    void onNavigationBarHide() throws RemoteException;

    void onNavigationBarShow() throws RemoteException;

    void onTransitionViewUpdate(int i) throws RemoteException;
}
